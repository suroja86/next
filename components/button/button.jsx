// import './button.scss';
import PropTypes from "prop-types";

const Button = () => {
    return(
        <button
            className={'loginButton'}
        >
            LOGIN
        </button>
    );
}

export default Button;